#pragma once
void matrixPow(float* m1Data, float*m2Data,float* dstData, int size);
void getK(float* tData,float* outData, int h, int w);
void copyRGB(float* rgbData, float* rgbaData,
			 int rows, int cols);

